import { ContrasenadorPipe } from './contrasenador.pipe';

describe('ContrasenadorPipe', () => {
  it('create an instance', () => {
    const pipe = new ContrasenadorPipe();
    expect(pipe).toBeTruthy();
  });
});
